#include<stdio.h>
main()
{
    int num, i, flag = 0;
    printf("num: ");
    scanf("%d", &num);

    for(i = 1; i <= num; i++)
    {
        if(num % i == 0)
        {
            flag++;
        }
    }

    if(flag == 2)
    {
        printf("prime\n");
    }
    else
    {
        printf("not prime\n");
    }
}
